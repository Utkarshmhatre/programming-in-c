#include <stdio.h>

void main()
{
    int num;
    printf("enter the no.:");
    scanf("%d",&num);
    if(num>=0)
    {
        printf("number is +ve");
    }
    else
    {
        printf("number is -ve");
    }
}